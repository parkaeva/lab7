#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a;
    cin>>a;
    setlocale(LC_ALL, " ");
    if (a%2==0 && a>=10 && a<=99)
    {
        cout<<"Истина";
    }else
    {
        cout<<"Ложь";
    }
    return 0;
}
